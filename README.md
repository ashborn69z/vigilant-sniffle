# Clone the boilerplate
git clone https://github.com/genlayerlabs/genlayer-project-boilerplate.git your-contract-name
cd your-contract-name

# Remove the existing git history
rm -rf .git

# Initialize your own repo
git init
git add .
git commit -m "Initial commit: GenLayer Intelligent Contract"
